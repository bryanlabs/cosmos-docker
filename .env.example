# THORChain Docker - Minimal Configuration Example
# 
# Quick Start:
#   cp thorchain-1.env .env    (for mainnet with all options)
#   cp .env.example .env       (for minimal setup)
#
# This file contains only the essential settings you're likely to change.
# For full configuration options, see thorchain-1.env

# Node Identity
MONIKER=my-thorchain-node
NETWORK=thorchain-1

# THORNode Version
THORNODE_VERSION=v3.6.1

# Data Storage (most important setting)
# Option 1: Docker volumes (default - simple)
# Leave DATA_DIR commented out
#
# Option 2: Custom path (production - dedicated disk)
# DATA_DIR=/mnt/data/blockchain

# Logging Level (info, warn, error, debug)
LOG_LEVEL=info
